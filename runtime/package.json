{
  "name": "horizon-runtime",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Reference runtime for Horizon OS: holds the model and the tools, so the browser never does.",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0"
  },
  "engines": { "node": ">=20" }
}
